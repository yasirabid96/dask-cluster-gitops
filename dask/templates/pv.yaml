apiVersion: v1
kind: PersistentVolume
metadata:
  name: dask-nfs-pv
spec:
  capacity:
    storage: "{{ .Values.nfs.size }}"
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  nfs:
    path: "{{ .Values.nfs.path }}"
    server: "{{ .Values.nfs.server }}"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dask-nfs-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: "{{ .Values.nfs.request }}"
